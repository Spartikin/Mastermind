import random
class Computer():
    def __init__ (self):
        pass

    def random_code (self):
        computer_random = random.randrange(1, 6) 
        code = computer_random
        while computer_random != 4:
            code.append + 1

        print(code)

# test = Computer()
# pr = test.random_code()

class Guess():
    guess = Peg[]



class Peg(Computer):
    

    def __init__ (self):
        pass
        
    def peg_colours(self, computer_random, code):
        if (computer_random == 1):{
            code.append ("R")
        }
        if (computer_random == 2): {
            code.append ("G")
        }
        if (computer_random == 3): {
            code.append ("L")
        }
        if (computer_random == 4): {
            code.append ("Y")
        }
        if (computer_random == 5): {
            code.append ("W")
        }
        if (computer_random == 6): {
            code.append ("B")
        }

code2 = Peg()
tt = code2.peg_colours()
print(code2)
